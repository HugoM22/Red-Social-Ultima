extends layout

block content
  .container.py-4
    h1.mb-4 Cambiar contraseña

    if error
      .alert.alert-danger= error
    if success
      .alert.alert-success= success

    form(method="post", action=`/usuarios/perfil/${usuarioId}/password`)
      .mb-3
        label.form-label(for="actual") Contraseña actual
        input.form-control(type="password", name="actual", id="actual", required)

      .mb-3
        label.form-label(for="nueva") Nueva contraseña
        input.form-control(type="password", name="nueva", id="nueva", required)

      .mb-3
        label.form-label(for="confirmar") Confirmar nueva contraseña
        input.form-control(type="password", name="confirmar", id="confirmar", required)

      button.btn.btn-primary(type="submit") Guardar cambios
