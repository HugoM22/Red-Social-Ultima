doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Página no encontrada – Artesanos.com
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
  body.d-flex.justify-content-center.align-items-center.bg-light(style="height:100vh")
    .text-center
      h1.display-4 404
      p.lead La página que buscas no existe o fue movida.
      a.btn.btn-primary.mt-3(href='/') Volver al inicio